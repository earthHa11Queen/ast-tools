CREATE TABLE IF NOT EXISTS ast_source_file_level_tmp(
    appName text,
    fileName text,
    directoryPath text,
    className text,
    importList text,
    lineCount text,
    methodCount text,
    variableCount text,
    constantCount text
);